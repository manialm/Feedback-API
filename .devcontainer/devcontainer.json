{
  // (Generated by Gemini)

  "name": "FastAPI Feedback API",
  // This tells Codespaces to use Docker Compose to build and run your environment.
  "dockerComposeFile": "../docker-compose.yml",
  
  // This specifies which service in the Docker Compose file is the main application.
  "service": "app",
  
  // This sets the default folder to open in the Codespace.
  "workspaceFolder": "/workspace",
  
  // Forward the port for the FastAPI app so you can access it from your browser.
  "forwardPorts": [8000],

  // Optional: Add VS Code extensions you want pre-installed.
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance"
      ]
    }
  }
}
